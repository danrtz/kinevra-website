---
import Layout from '../layouts/Layout.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<Layout title={SITE_TITLE} description={SITE_DESCRIPTION}>
  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-gradient"></div>
    <div class="hero-glow"></div>
    
    <div class="container">
      <div class="hero-layout">
        <div class="hero-content">
          <div class="hero-badge animate-slide-in">
            <span class="badge-dot"></span>
            <span>SAP BTP Specialists</span>
          </div>
          
          <h1 class="hero-title animate-slide-up">
            <span class="title-line">SAP Integration</span>
            <span class="title-line gradient-text-hdr">Done Right</span>
          </h1>
          
          <p class="hero-subtitle animate-slide-up" style="animation-delay: 0.1s;">
            We handle the complex SAP stuff so you don't have to. From S/4HANA migrations to BTP development, 
            we know the platform inside out.
          </p>
          
          <div class="hero-actions animate-slide-up" style="animation-delay: 0.2s;">
            <a href="mailto:daniel@kinevra.com" class="btn btn-primary btn-glow">Let's Talk</a>
            <a href="#services" class="btn btn-outline">What We Do</a>
          </div>
          
          <div class="hero-stats animate-slide-up" style="animation-delay: 0.3s;">
            <div class="stat">
              <div class="stat-number">100%</div>
              <div class="stat-label">CLOUD NATIVE</div>
            </div>
            <div class="stat">
              <div class="stat-number">24/7</div>
              <div class="stat-label">SUPPORT</div>
            </div>
            <div class="stat">
              <div class="stat-number">∞</div>
              <div class="stat-label">SCALABILITY</div>
            </div>
          </div>
        </div>
        
        <div class="hero-features">
          <div class="feature-card glass-card animate-slide-in" style="animation-delay: 0.2s;">
            <div class="feature-icon">⚡</div>
            <div class="feature-content">
              <h3>Same Day Response</h3>
              <p>Not weeks of emails</p>
            </div>
          </div>
          
          <div class="feature-card glass-card animate-slide-in" style="animation-delay: 0.3s;">
            <div class="feature-icon">🔒</div>
            <div class="feature-content">
              <h3>SOC2 Compliant</h3>
              <p>Your data stays yours</p>
            </div>
          </div>
          
          <div class="feature-card glass-card animate-slide-in" style="animation-delay: 0.4s;">
            <div class="feature-icon">📈</div>
            <div class="feature-content">
              <h3>99.9% Uptime</h3>
              <p>We monitor 24/7</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Services Section -->
  <section id="services" class="services">
    <div class="container">
      <div class="section-header">
        <h2 class="gradient-text">What We Actually Do</h2>
        <p>No buzzwords. Just real SAP work that gets done on time.</p>
      </div>
      
      <div class="grid grid-3">
        <div class="service-card glass-card">
          <div class="service-icon">
            <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 2L2 7L12 12L22 7L12 2Z"/>
              <path d="M2 17L12 22L22 17"/>
              <path d="M2 12L12 17L22 12"/>
            </svg>
          </div>
          <h3>SAP Integration</h3>
          <p>Got a mess of SAP systems that don't talk to each other? We'll fix that. S/4HANA, ECC, SCP - we've connected them all.</p>
          <ul class="service-features">
            <li>API Management</li>
            <li>Data Migration</li>
            <li>Process Automation</li>
            <li>Real-time Synchronization</li>
          </ul>
        </div>
        
        <div class="service-card glass-card">
          <div class="service-icon">
            <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M2 3H8C9.06087 3 10.0783 3.42143 10.8284 4.17157C11.5786 4.92172 12 5.93913 12 7V21C12 20.2044 11.6839 19.4413 11.1213 18.8787C10.5587 18.3161 9.79565 18 9 18H2V3Z"/>
              <path d="M22 3H16C14.9391 3 13.9217 3.42143 13.1716 4.17157C12.4214 4.92172 12 5.93913 12 7V21C12 20.2044 12.3161 19.4413 12.8787 18.8787C13.4413 18.3161 14.2044 18 15 18H22V3Z"/>
            </svg>
          </div>
          <h3>BTP Development</h3>
          <p>Need custom apps on BTP? We build them. CAP, RAP, Fiori - pick your acronym, we know it.</p>
          <ul class="service-features">
            <li>Cloud Foundry Apps</li>
            <li>Integration Suite</li>
            <li>Extension Suite</li>
            <li>Analytics Cloud</li>
          </ul>
        </div>
        
        <div class="service-card glass-card">
          <div class="service-icon">
            <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="10"/>
              <path d="M8 14S9.5 16 12 16S16 14 16 14"/>
              <line x1="9" y1="9" x2="9.01" y2="9"/>
              <line x1="15" y1="9" x2="15.01" y2="9"/>
            </svg>
          </div>
          <h3>Fix & Support</h3>
          <p>When things break at 2 AM (they always do), we answer the phone. Real support from people who actually know SAP.</p>
          <ul class="service-features">
            <li>Architecture Design</li>
            <li>Best Practices</li>
            <li>Performance Optimization</li>
            <li>24/7 Support</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Technology Stack -->
  <section class="tech-stack">
    <div class="container">
      <div class="section-header">
        <h2 class="gradient-text">Tech We Work With</h2>
        <p>The actual tools we use every day (not just logos for the website)</p>
      </div>
      
      <div class="tech-grid">
        <div class="tech-item">
          <div class="tech-logo">SAP</div>
          <span>S/4HANA</span>
        </div>
        <div class="tech-item">
          <div class="tech-logo">BTP</div>
          <span>Business Technology Platform</span>
        </div>
        <div class="tech-item">
          <div class="tech-logo">CF</div>
          <span>Cloud Foundry</span>
        </div>
        <div class="tech-item">
          <div class="tech-logo">K8s</div>
          <span>Kubernetes</span>
        </div>
        <div class="tech-item">
          <div class="tech-logo">API</div>
          <span>REST & OData</span>
        </div>
        <div class="tech-item">
          <div class="tech-logo">AI</div>
          <span>Machine Learning</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Why Choose Section -->
  <section class="why-choose">
    <div class="container">
      <div class="section-header">
        <h2 class="gradient-text">Why Work With Us</h2>
        <p>Small team. Big experience. No nonsense.</p>
      </div>
      
      <div class="grid grid-2">
        <div class="why-card">
          <div class="why-number">01</div>
          <h3>Direct Access</h3>
          <p>You work directly with me. No account managers, no junior developers learning on your dime. Just experienced hands on your project.</p>
        </div>
        
        <div class="why-card">
          <div class="why-number">02</div>
          <h3>SAP Veteran</h3>
          <p>I've been in the SAP world long enough to know what works and what's just vendor hype. I'll save you from the expensive mistakes.</p>
        </div>
        
        <div class="why-card">
          <div class="why-number">03</div>
          <h3>Fixed Timeline</h3>
          <p>When I say 6 weeks, I mean 6 weeks. Not 6 months with change requests. You'll know exactly what you're getting and when.</p>
        </div>
        
        <div class="why-card">
          <div class="why-number">04</div>
          <h3>Plain English</h3>
          <p>No technical jargon unless you want it. I explain things in a way that makes sense to humans, not just SAP consultants.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section id="contact" class="cta">
    <div class="container">
      <div class="cta-content glass-card">
        <h2 class="gradient-text">Got an SAP Problem?</h2>
        <p>Tell me what's broken and I'll tell you if I can fix it. No sales pitch, just straight answers.</p>
        
        <div class="cta-actions">
          <a href="mailto:daniel@kinevra.com" class="btn btn-primary">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="display: inline-block; margin-right: 8px;">
              <path d="M4 4H20C21.1 4 22 4.9 22 6V18C22 19.1 21.1 20 20 20H4C2.9 20 2 19.1 2 18V6C2 4.9 2.9 4 4 4Z"/>
              <path d="M22 6L12 13L2 6"/>
            </svg>
            Email Me
          </a>
          <div class="contact-info">
            <span>Quick response guaranteed:</span>
            <a href="mailto:daniel@kinevra.com" class="email-link">daniel@kinevra.com</a>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  main {
    width: 100%;
    max-width: none;
    margin: 0;
    padding: 0;
  }

  /* Hero Section with HDR support */
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
    padding: 120px 0 80px;
    background: #000;
  }

  /* HDR Gradient Background */
  .hero-gradient {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(ellipse at 30% 0%, 
      color(display-p3 1 0 0 / 0.5) 0%, 
      transparent 50%),
      radial-gradient(ellipse at 80% 100%, 
      color(display-p3 0.5 0 0.8 / 0.3) 0%, 
      transparent 50%);
    mix-blend-mode: screen;
    animation: gradientShift 10s ease-in-out infinite;
  }

  /* Fallback for non-HDR displays */
  @supports not (color: color(display-p3 1 0 0)) {
    .hero-gradient {
      background: radial-gradient(ellipse at 30% 0%, 
        rgba(255, 0, 0, 0.5) 0%, 
        transparent 50%),
        radial-gradient(ellipse at 80% 100%, 
        rgba(127, 0, 204, 0.3) 0%, 
        transparent 50%);
    }
  }

  .hero-glow {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 800px;
    height: 800px;
    transform: translate(-50%, -50%);
    background: radial-gradient(circle, 
      rgba(255, 0, 0, 0.1) 0%, 
      transparent 70%);
    filter: blur(60px);
    pointer-events: none;
  }

  @keyframes gradientShift {
    0%, 100% { transform: scale(1) rotate(0deg); }
    50% { transform: scale(1.1) rotate(5deg); }
  }

  .hero-layout {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 80px;
    align-items: center;
    position: relative;
    z-index: 2;
  }

  .hero-content {
    max-width: 800px;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 20px;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 100px;
    margin-bottom: 32px;
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
  }

  .badge-dot {
    width: 8px;
    height: 8px;
    background: #00ff00;
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
    box-shadow: 0 0 10px rgba(0, 255, 0, 0.5);
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.6; transform: scale(1.2); }
  }

  .hero-title {
    font-size: clamp(3rem, 8vw, 5.5rem);
    font-weight: 900;
    line-height: 1.1;
    margin-bottom: 32px;
    letter-spacing: -0.03em;
    max-width: 100%;
  }

  .title-line {
    display: block;
    line-height: 1;
    padding-bottom: 0.1em;
  }

  /* HDR Gradient Text */
  .gradient-text-hdr {
    background: linear-gradient(135deg, 
      color(display-p3 1 0.2 0.4) 0%,
      color(display-p3 0.8 0 1) 50%,
      color(display-p3 0.4 0.2 1) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    filter: brightness(1.2) contrast(1.1);
  }

  /* Fallback gradient for non-HDR */
  @supports not (color: color(display-p3 1 0 0)) {
    .gradient-text-hdr {
      background: linear-gradient(135deg, 
        #ff3366 0%,
        #cc00ff 50%,
        #6633ff 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
  }

  .hero-subtitle {
    font-size: 1.25rem;
    line-height: 1.6;
    margin-bottom: 40px;
    color: rgba(255, 255, 255, 0.7);
    max-width: 500px;
  }

  .hero-actions {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
    margin-bottom: 60px;
  }

  .btn-glow {
    position: relative;
    background: linear-gradient(135deg, #007AFF, #5856D6);
    box-shadow: 0 4px 20px rgba(0, 122, 255, 0.4);
  }

  .btn-glow::before {
    content: '';
    position: absolute;
    top: -2px;
    left: -2px;
    right: -2px;
    bottom: -2px;
    background: linear-gradient(135deg, #007AFF, #5856D6, #FF3B30);
    border-radius: 100px;
    opacity: 0;
    z-index: -1;
    transition: opacity 0.3s ease;
    filter: blur(10px);
  }

  .btn-glow:hover::before {
    opacity: 1;
  }

  .btn-glow:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(0, 122, 255, 0.6);
  }

  .hero-stats {
    display: flex;
    gap: 60px;
  }

  .stat {
    text-align: left;
  }

  .stat-number {
    font-size: 2.5rem;
    font-weight: 900;
    letter-spacing: -0.02em;
    background: linear-gradient(135deg, #007AFF, #5856D6);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .stat-label {
    color: rgba(255, 255, 255, 0.5);
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    margin-top: 4px;
    font-weight: 600;
  }

  /* Feature Cards */
  .hero-features {
    display: flex;
    flex-direction: column;
    gap: 20px;
    min-width: 320px;
  }

  .feature-card {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 20px 24px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 16px;
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    transition: all 0.3s ease;
  }

  .feature-card:hover {
    background: rgba(255, 255, 255, 0.06);
    border-color: rgba(255, 255, 255, 0.15);
    transform: translateX(-8px);
    box-shadow: 0 8px 32px rgba(0, 122, 255, 0.15);
  }

  .feature-icon {
    font-size: 1.5rem;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 10px;
  }

  .feature-content h3 {
    font-size: 1rem;
    font-weight: 600;
    margin-bottom: 4px;
    color: #fff;
  }

  .feature-content p {
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.5);
    margin: 0;
  }

  /* Services Section */
  .services {
    background: rgba(255, 255, 255, 0.02);
  }

  .section-header {
    text-align: center;
    margin-bottom: 60px;
  }

  .section-header h2 {
    margin-bottom: 16px;
  }

  .service-card {
    position: relative;
    height: 100%;
    display: flex;
    flex-direction: column;
  }

  .service-icon {
    width: 64px;
    height: 64px;
    background: var(--gradient-main);
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 24px;
    color: white;
    position: relative;
    overflow: hidden;
  }

  .service-icon::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
    transition: left 0.5s ease;
  }

  .service-card:hover .service-icon::before {
    left: 100%;
  }

  .service-card h3 {
    font-size: 1.5rem;
    margin-bottom: 16px;
  }

  .service-features {
    list-style: none;
    margin-top: auto;
    padding-top: 24px;
  }

  .service-features li {
    padding: 8px 0;
    color: var(--text-secondary);
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .service-features li::before {
    content: '✓';
    color: var(--success);
    font-weight: bold;
  }

  /* Tech Stack */
  .tech-stack {
    padding: 80px 0;
  }

  .tech-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 24px;
    margin-top: 48px;
  }

  .tech-item {
    text-align: center;
    padding: 24px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 16px;
    transition: all 0.3s ease;
  }

  .tech-item:hover {
    transform: translateY(-4px);
    background: var(--surface-hover);
    border-color: var(--primary);
  }

  .tech-logo {
    font-size: 2rem;
    font-weight: 900;
    background: var(--gradient-main);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 8px;
  }

  .tech-item span {
    font-size: 0.875rem;
    color: var(--text-secondary);
  }

  /* Why Choose */
  .why-choose {
    background: rgba(255, 255, 255, 0.02);
    padding: 100px 0;
  }

  .why-card {
    padding: 32px;
    position: relative;
  }

  .why-number {
    font-size: 4rem;
    font-weight: 900;
    background: var(--gradient-main);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    opacity: 0.3;
    position: absolute;
    top: 0;
    left: 32px;
  }

  .why-card h3 {
    margin-top: 32px;
    margin-bottom: 16px;
  }

  /* CTA Section */
  .cta {
    padding: 100px 0;
    position: relative;
  }

  .cta-content {
    text-align: center;
    padding: 60px;
    background: var(--gradient-main);
    position: relative;
    overflow: hidden;
  }

  .cta-content::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.3);
    z-index: 1;
  }

  .cta-content > * {
    position: relative;
    z-index: 2;
  }

  .cta-content h2 {
    margin-bottom: 16px;
  }

  .cta-content p {
    max-width: 600px;
    margin: 0 auto 32px;
    color: rgba(255, 255, 255, 0.9);
  }

  .cta-actions {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 24px;
  }

  .contact-info {
    display: flex;
    align-items: center;
    gap: 12px;
    color: rgba(255, 255, 255, 0.8);
  }

  .email-link {
    color: white;
    font-weight: 600;
    text-decoration: underline;
  }

  /* Responsive */
  @media (max-width: 1200px) {
    .hero-layout {
      grid-template-columns: 1fr;
      gap: 60px;
    }

    .hero-features {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 16px;
      width: 100%;
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: 100px 0 60px;
      min-height: auto;
    }

    .hero-content {
      text-align: center;
    }

    .hero-badge {
      margin: 0 auto 24px;
    }

    .hero-title {
      font-size: clamp(2.5rem, 12vw, 4rem);
    }

    .hero-subtitle {
      font-size: 1.125rem;
      margin: 0 auto 32px;
    }

    .hero-stats {
      gap: 30px;
      justify-content: center;
    }

    .stat {
      text-align: center;
    }

    .stat-number {
      font-size: 1.75rem;
    }

    .hero-features {
      grid-template-columns: 1fr;
      margin-top: 40px;
    }

    .hero-actions {
      flex-direction: column;
      align-items: center;
    }

    .hero-actions .btn {
      width: 100%;
      max-width: 280px;
      text-align: center;
    }

    .cta-content {
      padding: 40px 24px;
    }

    .tech-grid {
      grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
    }
  }

  @media (max-width: 480px) {
    .hero-badge {
      font-size: 0.875rem;
      padding: 6px 16px;
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .hero-stats {
      flex-direction: column;
      gap: 24px;
    }
  }
</style>
